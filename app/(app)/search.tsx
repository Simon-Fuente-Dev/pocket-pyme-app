import {Text} from "tamagui";

type SearchProps = {
    searchText: string;
}
export default function SearchScreen ({searchText}: SearchProps) {
    return (
        <Text>Pagina de busqueda</Text>
    )
}